{
  "name": "Matrix Rain",
  "description": "A cascading green code effect inspired by The Matrix.",
  "author": "Charles",
  "version": "1.0",
  "canvas": {
    "width": 800,
    "height": 600,
    "background": "#000000"
  },
  "code": "function(ctx, canvas) {\n  const cols = canvas.width / 20;\n  const yPositions = Array(Math.floor(cols)).fill(0);\n  const fontSize = 18;\n\n  function draw() {\n    ctx.fillStyle = 'rgba(0, 0, 0, 0.05)';\n    ctx.fillRect(0, 0, canvas.width, canvas.height);\n\n    ctx.fillStyle = '#0F0';\n    ctx.font = `${fontSize}px monospace`;\n\n    yPositions.forEach((y, i) => {\n      const text = String.fromCharCode(0x30A0 + Math.random() * 96);\n      const x = i * 20;\n      ctx.fillText(text, x, y);\n\n      if (y > canvas.height + Math.random() * 10000) {\n        yPositions[i] = 0;\n      } else {\n        yPositions[i] = y + fontSize;\n      }\n    });\n\n    requestAnimationFrame(draw);\n  }\n\n  draw();\n}"
}
